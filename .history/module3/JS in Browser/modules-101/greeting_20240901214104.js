export const informalGreeting = (person) => {
    console.log(`what's up ${person}.`)
}
export const formalGreeting = () => {
    for(let i = 0; i < 2; i++)
        console.log(`Good morning Gentelman.`)
}

export default greeting